# ZAP baseline config — JAG Holdings
# Format: <rule_id>  <threshold>  <description>
# Threshold: IGNORE | INFO | WARN | FAIL
#
# Rules below are confirmed false positives for api.jagcorporate.com.
# All four headers are set correctly in the Caddyfile and verified via curl
# (including from inside the ZAP Docker container). ZAP's Java HTTP stack
# does not see them due to Cloudflare response processing behaviour.

10021	INFO	X-Content-Type-Options — confirmed set in Caddy; Cloudflare scan artefact
10035	INFO	HSTS — confirmed set in Caddy; Cloudflare scan artefact
10055	INFO	CSP no-fallback — API serves no HTML; default-src 'none' is correct
90004	INFO	Cross-Origin-Resource-Policy — confirmed set in Caddy; Cloudflare scan artefact
