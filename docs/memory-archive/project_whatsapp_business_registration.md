---
name: project-whatsapp-business-registration
description: "WhatsApp Business Cloud API number (868-277-3726) is fully live/production; INBOUND webhook fully fixed 2026-07-13; ALL 32 OUTBOUND templates now APPROVED by Meta as of 2026-07-19 (previously pending) — outbound notifications fully live"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2a2ff520-2da0-4ab8-99cf-20685c422028
---

**UPDATE 2026-07-19 — ALL templates now APPROVED, outbound fully live.** Checked live via `GET /{WABA}/message_templates?fields=name,status` (WABA `1345040596988860`) while investigating an unrelated viewing-booking bug fix: all 32 templates (the 30 from session 40 plus the 2 `_v2` receipt templates) show `status: APPROVED`. This resolves the long-pending blocker described throughout this memory below — no further Meta review wait is needed. The 10 "CODE CHANGES STILL NEEDED" items listed further down (URL-button param fixes, param-count alignment, etc.) were NOT re-verified as actually applied in this check — only template *approval status* was confirmed. Before relying on any specific template's param shape, verify the corresponding call site still matches what was approved (grep the route file, not this memory). See [[project-viewing-booking-pipeline-fixes]] for the session this was discovered in.

**UPDATE session 41 (2026-07-13) — INBOUND webhook now fully fixed and verified live; this is separate from the outbound-template-approval issue below.** Inbound WhatsApp (prospect replies → enquiry creation + auto-reply) had never processed a single message since the feature was built — a chain of 5 independent bugs (app unpublished, WABA not subscribed to the app, global `express.json()` eating the body before HMAC verification, signature checked against the wrong secret (access token instead of App Secret), and a second body-parsing bug that left `req.body` empty even after signature checks passed). All fixed, deployed, and confirmed with a real message from Robert's phone creating a real enquiry + auto-reply. Full writeup: [[feedback-webhook-signature-testing-pattern]]. **Do not confuse this with the still-open outbound-template-approval blocker below** — inbound webhook infra and outbound template sends are two completely independent problems; inbound is now solved, outbound (Meta template review) may still be pending.

**UPDATE session 40 (2026-07-11) — number is confirmed LIVE; the real gap is missing templates.** Tested end-to-end via the listing "broadcast enquiry" path (temp enquiry against Apt C1 / 45 Eleventh Street, real enquirer lookup, send restricted to test number `18682912787` only — never spam real prospects). Findings:
- The "delivery pending propagation" worry from session 39 is **resolved**. Sending a `hello_world` probe returned `(#131058) Hello World templates can only be sent from the Public Test Numbers` — Meta accepted & processed the request and only blocked hello_world by policy, which proves `868-277-3726` is now a fully-provisioned **production** sending number. Token is valid (`debug_token`: SYSTEM_USER, app "JAG Platform" `908891082236842`, scopes `whatsapp_business_management`+`whatsapp_business_messaging`, `is_valid:true`), env vars are wired into the container, number is registered (no `#133010`).
- **The actual blocker:** every real send failed with `(#132001) template name (jag_adv_new_listing) does not exist in en / en_US / en_GB`. The code drives ALL notifications through `sendTemplate` (~29 distinct templates: `jag_adv_*`, `jag_enq_*`, `jag_onb_*`, `jag_mnt_*`, `jag_ren_*`, `jag_rent_*`), and none can be confirmed to exist in Meta — most/all were almost certainly never created, since the send path was a silent no-op until session 39 wired the env. So NO templated WhatsApp notification works today; only free-text `sendText` (24h customer-service window) does.
- **WABA id = `1345040596988860`** (account name "JAG Properties", business_id `2898193900525983`, status Approved but **business verification = Unverified**). NOT stored in `.env` (only `WHATSAPP_PHONE_NUMBER_ID=1184193838112120` is); the System User token can't traverse `/me/businesses` (`Missing Permission` — lacks `business_management`) nor the phone→WABA edge, so Robert read it from Business Settings → WhatsApp Accounts. With the WABA id + the existing `whatsapp_business_management` scope, templates CAN be enumerated (`GET /{WABA}/message_templates`) and created (`POST /{WABA}/message_templates`) directly with the container's token — verified working.
- **Two structural code issues found that mean it's NOT just "create 30 templates"** — code + templates must be co-designed: (1) **URL-button mismatch** — `jag_adv_new_listing`, `jag_enq_auto_reply`, `jag_enq_screening_approved` attach a URL button but the code passes the FULL url (`https://jagcorporate.com/book/<slug>`) as the button variable; Meta only allows a static domain + variable suffix, so the template button URL must be `https://jagcorporate.com/book/{{1}}` and those 3 call sites must be changed to send only the `<slug>`/token, not the full URL. (2) **`jag_enq_post_viewing` param conflict** — sent with 2 params from `enquiries.ts` (name, application_link) but 1 param from `viewings.ts` (name); one caller will always fail — needs a template split or the two call sites unified.
- **Session 40 plan chosen by Robert = "prove with 2-3 first."** Created + submitted 3 proof templates (all `en_US`) via `POST /{WABA}/message_templates`: `jag_adv_new_listing` (MARKETING, id 1576397840807327, 4 body vars + `…/book/{{1}}` URL button), `jag_rent_receipt_full` (UTILITY, id 1686079415937788, 7 body vars), `jag_rent_reminder_d1` (UTILITY, id 1319281970291837, 9 body vars). **All 3 stuck PENDING for 1h+ (2026-07-11 ~12:09–14:04 UTC)** — likely slow first-template review on the unverified/new WABA; not rejected, just queued. **Resume:** re-check `GET /{WABA}/message_templates?fields=name,status`; when APPROVED, run the staged delivery test (send each to test number `18682912787` with CORRECT params — for the URL button send the SLUG ONLY `apt-c1-df2c43d2`, not the full URL) to confirm real delivery. Then build the remaining 27 templates + make the 2 code fixes + deploy. Full 30-template param spec was extracted from every call site this session (in listing/enquiries/viewings/applications/deposits/handover/maintenance-tickets/renewals/rent-schedule.ts). **How to apply:** WhatsApp is "live" only for the number, not notifications — nothing works until templates are APPROVED (not just created). Completing business verification would raise messaging limits & likely speed template review.

**Robert's template-content decisions (session 40) — apply to the whole build:**
- **Public advertisement (`jag_adv_new_listing`) must NOT show the full street address** (security). Show **area/town only** (e.g. "Barataria"), not "45 Eleventh Street". Revised body: "Hello! A new rental is now available in {{1}}. Rent: TTD ${{2}} per month. Available from: {{3}}. Tap below to see photos, description and book a viewing." + URL button `…/book/{{1}}`. When wiring code, pass the property's **town/area**, not `property_name`. (Private-to-tenant templates like rent reminders CAN show full property/unit — the address-privacy rule is public-broadcast only.)
- **Bank payment details = variables filled from app config, NOT hardcoded** into the template (keeps the real account number off Meta's servers + out of transcripts; change in one place, no template re-approval). The payment block is: payee name / Bank / Account Type / Account No — e.g. Robert Johnson-Attin · First Citizens Bank · Chequing · <acct no>. Revised `jag_rent_reminder_d1` now has 11 body vars: {{1}}tenant {{2}}property {{3}}unit {{4}}amount {{5}}due {{6}}payee {{7}}bank {{8}}acctType {{9}}acctNo {{10}}refName {{11}}refUnit. **This same expanded payment block should be applied to the other pay-to templates** (jag_rent_missed_d1, jag_rent_formal_demand, jag_rent_legal_notice, jag_onb_welcome_pack) during the full build, and the sending code updated to pass payee+type+acctNo (currently only bank+acct are passed).
- **Viewing confirmation (`jag_enq_viewing_confirm`) should include a GPS pin/location** once a prospect passes screening and a viewing is booked (WhatsApp supports either a `location` message or a Google-Maps link in the template body). Build this into that template during rollout.

**Template management permission gap (session 40):** the jag-api System User token can **create** templates (`POST /{WABA}/message_templates`) but **cannot delete** them (`DELETE` → `(#100) Need permission on either WhatsApp Business Account or owner/shared business`), and you can't create a 2nd `en_US` version while a PENDING one exists ("Content in This Language Already Exists"). Fix = grant the **jag-api system user Full control** on the JAG Properties WhatsApp account (Business Settings → System Users → jag-api → Assigned assets → WhatsApp accounts → Full control). Robert chose to do this. Until then, editing a template requires either UI deletion or waiting for it to leave PENDING (approved/rejected templates CAN be edited via API; pending ones cannot). Revised #1 + #3 payloads are staged in scratchpad `wa-resubmit.js`/`wa-create-revised.js`; delivery-test in `wa-send-proof.js` (updated to the revised param shapes).

**Full-control granted + delete-lock gotcha (session 40, later):** Robert assigned the jag-api system user **Full control ("Everything")** on the JAG Properties WhatsApp account asset (Business Settings → System Users → jag-api → Add assets → WhatsApp accounts → JAG Properties → Full access). Confirmed: `DELETE /{WABA}/message_templates?name=X` now returns `{"success":true}`. BUT — **deleting a template that is still PENDING locks that name+language for an extended period** (`error_subcode 2388023` "Message template language is being deleted" on recreate; Meta's "try again in <1 min" is wrong — it persisted 13+ min and likely clears only when the original pending review finalizes, possibly hours). The deleted templates vanish from the list immediately but the name stays reserved. **Lesson for the full build: create each template ONCE with final, Robert-approved copy; do NOT submit-then-delete-and-recreate a pending template.** If copy must change, either wait for it to leave PENDING then EDIT (editing approved/rejected templates is allowed), or use a fresh name. As of session-40 end: `jag_adv_new_listing` + `jag_rent_reminder_d1` were deleted (revised copy pending recreate once lock clears — recreate via `wa-create-revised.js`); `jag_rent_receipt_full` still PENDING/unchanged; `hello_world` default. **Resume:** recreate the 2 revised (retry `wa-create-revised.js` until no 2388023), then wait for all 3 to APPROVE, then run `wa-send-proof.js` delivery test to `18682912787`.

**Session 40 continuation — FULL SET created + ALL code changes done (not yet deployed):**
- **29 of 30 templates now exist on WABA `1345040596988860`, all PENDING** (created with the reviewed copy). Verified every one's body-var count + button URL against the code — they match exactly (auto_reply=2 no btn, screening_approved=1 + `…/schedule/{{1}}` btn, viewing_confirm=4 + `maps.google.com/?q={{1}}` btn, lease_ready=4 + `sign.jagcorporate.com/{{1}}` btn, condition_report=1 + sign btn, welcome_pack=10, missed_d1=10, formal_demand=12, legal_notice=12, reminder_d1=11, etc.). `jag_rent_reminder_d1` recreated OK (id 4635344836712016).
- **STILL missing: `jag_adv_new_listing`** — its name+language delete-lock (2388023) is stuck far longer than reminder_d1's (which cleared). A background retry is running; **if it stays locked, create under a new name (e.g. `jag_adv_new_listing_v2`) and update the ONE code ref in `listing.ts` broadcastNewListing.** Template content: 3 body vars (area, rent, avail) + URL button `https://jagcorporate.com/book/{{1}}` (slug only).
- **ALL code changes DONE and typecheck-clean (`npx tsc --noEmit` exit 0), NOT yet deployed.** New file `jag-api/src/lib/payment-config.ts` (`getPaymentDetails()` reads env `JAG_BANK_ACCOUNT_NAME`/`JAG_BANK_NAME`/`JAG_BANK_ACCOUNT_TYPE`/`JAG_BANK_ACCOUNT_NO`). Changed: `lib/whatsapp.ts` default languageCode `en`→`en_US` (+ 5 explicit `'en'`→`'en_US'` in viewings/rent-schedule/renewals); `listing.ts` (area/rent/avail + slug btn, city from prop_properties.city); `enquiries.ts` (auto_reply 2-param no-btn + city, screening_approved token-only btn); `viewings.ts` (viewing_confirm + maps btn via encodeURIComponent(address) since NO gps cols exist; post_viewing +app link placeholder `PUBLIC_APPLY_BASE_URL`); `properties.ts` (lease send-for-signing switched from `sendText`→`sendTemplate('jag_onb_lease_ready')` + sign-path btn — this is now the CANONICAL lease-signing WA send); `applications.ts` send-lease-signed template send DISABLED (no signing link there → would emit broken btn; returns `{sent:false}`); `handover.ts` (condition_report 1-body+sign btn, welcome_pack payment block); `rent-schedule.ts` (reminder_d1 11, missed_d1 10, formal/legal 12 via getPaymentDetails()). Documenso signing URL = `https://sign.jagcorporate.com/sign/<token>`; button gets path only via `.replace(/^https?:\/\/[^/]+\//,'')`.
- **DEPLOYED 2026-07-11 (session 40).** All 30 templates now exist: the stuck `jag_adv_new_listing` name-lock never cleared, so it was recreated as **`jag_adv_new_listing_v2`** (id 832710609777171) and `listing.ts` broadcastNewListing points at that name. Code built + deployed (docker compose build api + up -d --force-recreate api); health `ready`; env verified live in container. Payment/contact env added to `/opt/jag/jag-infra/.env` AND wired into `docker-compose.yml` api block: `JAG_BANK_ACCOUNT_NAME=Robert Johnson-Attin`, `JAG_BANK_NAME=First Citizens Bank`, `JAG_BANK_ACCOUNT_TYPE=Chequing`, `JAG_BANK_ACCOUNT_NO=2503082`, `JAG_MANAGER_NAME=Robert`, **`JAG_MANAGER_PHONE=JAG_OWNER_PHONE=868-753-2637`** (the call-us number; WhatsApp sender 868-277-3726 is a separate line committed to the WABA), `PUBLIC_APPLY_BASE_URL=https://jagcorporate.com/apply`. `.env` backed up to `.env.bak-*`. **Deploy was NOT via deploy.sh (avoided its git auto-commit of unrelated WIP)** — manual build:prod → scp dist + docker-compose.yml → build/force-recreate.
- **Public `/apply` Stage-2 rental application form — BUILT + DEPLOYED 2026-07-11 (session 40).** New `routes/properties/public-apply.ts` (`publicApplyRouter` at `/api/v1/public/apply`): `GET /:token` (resolve enquiry→unit/property + prefill), `POST /:token/upload-url` (presigned MinIO PUT to BUCKET_DOCUMENTS for ID/payslips/job letter), `POST /:token` (writes `prop_applications` PENDING + `prop_application_documents`, burns token, enquiry→APPLICATION_RECEIVED, fires "New rental application" bell notification). Migration **045** (`prop_enquiries.application_token`/`_expires_at`/`application_submitted_at`; applied + registered in `__migrations`). `viewings.ts` send-post-viewing-links now generates a one-time 30-day token per prospect → links `https://jagcorporate.com/apply/<token>`. Frontend `pages/PublicApply.tsx` (public route split in `App.tsx`, bypasses Keycloak like `/book`). Deployed (migration → API build/force-recreate → frontend scp); verified: health ready, `GET /public/apply/<bad>` → clean NOT_FOUND envelope, `/apply/<token>` serves SPA. **Note:** the manual `enquiries.ts` send-app-link endpoint still takes a pasted URL (not upgraded to generate a token) — only the automated post-viewing path issues real tokens.
- **Landlord-on-receipts fix — DEPLOYED 2026-07-11 (session 40).** Robert clarified tenancy agreements + receipts are with **Robert Johnson-Attin personally**, not JAG Properties (the lease PDF `lib/lease-pdf.ts` ALREADY correctly names him as Landlord — no change). Chose "keep JAG Properties brand + name Robert as landlord/received-by". Changed: rent receipt HTML (`rent-schedule.ts generateRentReceiptHtml`) + deposit receipt HTML (`deposits.ts generateDepositReceiptHtml`) — added "Landlord: Robert Johnson-Attin" row + "Received with thanks by Robert Johnson-Attin, Landlord" footer, kept the "JAG Properties Management Ltd" brand header; frontend WhatsApp receipt text (`PropertiesPanel.tsx formatReceipt`) — added landlord line + "Received by Robert Johnson-Attin, Landlord". The 2 WhatsApp receipt templates got the same line — **but deleting the PENDING originals name-locked them AGAIN (2388023, same bug), so they were recreated as `jag_rent_receipt_full_v2` (id 1047072684521876) + `jag_onb_deposit_receipt_v2` (id 1513688413888767)** and code (rent-schedule.ts, deposits.ts) points at the `_v2` names. **LESSON REINFORCED: never delete a PENDING template — it locks the name for a long time. Wait for APPROVED then edit, or create under a new name from the start.** Deployed (API rebuild + frontend); health ready. So `_v2`-renamed templates in code now: `jag_adv_new_listing_v2`, `jag_rent_receipt_full_v2`, `jag_onb_deposit_receipt_v2`.
- **REMAINING (WhatsApp):** **wait for Meta to APPROVE the templates** (all PENDING; new/unverified WABA, slow first review — the 2 receipt `_v2`s are freshest), then run `wa-send-proof.js` delivery test to `18682912787`. Code is live but WhatsApp notifications still won't SEND until templates leave PENDING → APPROVED. Session-40 WhatsApp+/apply work was committed/pushed (`42a69c1`); the later receipt/landlord changes are deployed but **not yet committed**.

**Session-40 (later still) — FULL SET of 30 templates created with Robert-approved copy.** After reviewing all copy, Robert said "create full set." Batch-created 27 templates via `wa-create-all.js` (+ `wa-create-fix5.js` for 5 that first failed Meta rules). **Meta template rules learned the hard way:** (a) a variable cannot be the first OR last thing in the body — must have static text after the final `{{n}}` (fix: end lines like "Call {{5}} anytime." / "...to arrange payment."); (b) too many variables for body length is rejected ("too many variables for its length") — pad with static wording (hit on `jag_mnt_status_update`, 7 vars). **Current state: 28 of 30 JAG templates created & PENDING** (all 27 batch + `jag_rent_receipt_full`); the only 2 missing are `jag_adv_new_listing` + `jag_rent_reminder_d1`, still blocked by the 2388023 name-lock (retry `b6pj6i8w0` failing ~2h; lock persists until Meta finalizes the deleted originals — may take many hours / next day). **Approved copy decisions applied:** `jag_enq_auto_reply` = area-only, NO booking button (booking only surfaces after pre-screening); legal notices (`jag_rent_formal_demand`/`legal_notice`) + `jag_rent_missed_d1` + `jag_onb_welcome_pack` all carry the full payment block (payee/bank/type/acctNo as config vars). Payment details are config variables everywhere (not hardcoded). GPS "Open in Maps" URL button on `jag_enq_viewing_confirm`; "Review & Sign" sign.jagcorporate.com button on `jag_onb_lease_ready` + `jag_condition_report_signing_request`; "Book Viewing" schedule button on `jag_enq_screening_approved`.

**⚙️ CODE CHANGES STILL NEEDED (not yet done — templates exist in Meta, code must be aligned then deployed) before these actually send correctly:** (1) all 3 URL-button sites send SLUG/TOKEN only, not full URL (`jag_adv_new_listing`, `jag_enq_screening_approved`→schedule token, `jag_enq_auto_reply`→now has no button so drop it); (2) `jag_enq_auto_reply` pass area (town) not property_name, drop unit param → 2 params; (3) `jag_enq_post_viewing` unify to 2 params (name, app link) — fix `viewings.ts` which sends 1; (4) `jag_enq_viewing_confirm` add maps-coords button param; (5) `jag_onb_lease_ready` + `jag_condition_report_signing_request` add signing-token button param; (6) `jag_onb_welcome_pack` add payee/type/acctNo params (10 total); (7) `jag_rent_missed_d1` add full payment block params (10 total); (8) `jag_rent_formal_demand`/`jag_rent_legal_notice` add payee/bank/type/acctNo params (12 total); (9) `jag_rent_partial_payment` reorder to name/unit/paid/balance/paiddate/penalty/phone; (10) `jag_mnt_status_update` matches new 7-var wording. Payee/bank/acctType/acctNo should come from a single config source (env or a settings row) so they're maintained in one place. Full param spec for every template is in scratchpad `wa-create-all.js`/`wa-create-fix5.js`/`wa-create-revised.js`.

**NEW BUILD QUEUED (Robert approved):** a public Stage-2 rental application form at `jagcorporate.com/apply/<token>` writing to `prop_applications` (jag_properties) — personal/employment/income fields + document uploads (national ID, payslips ×3, employment letter, 2 references, prior landlord), mirroring the public booking page pattern (`pages/PublicBooking.tsx` + a public route in `App.tsx` bypassing Keycloak). The `jag_enq_post_viewing` template links to it. Currently there is NO public application form — only the admin panel (`PropertiesApplicationsPanel.tsx`) + data model (migration 015). Stage-1 pre-screening questionnaire already exists on the booking page (`prop_enquiries.screening_answers`, migration 039).

Discovered during Documenso live-testing (session 39, 2026-07-08) that WhatsApp sending had never actually worked in production — see [[feedback-docker-compose-env-wiring-gap]] for the root cause (env vars never wired into `docker-compose.yml`).

**Fixed and completed this session:**
- Added `WHATSAPP_ACCESS_TOKEN`/`WHATSAPP_PHONE_NUMBER_ID` to `docker-compose.yml`'s `api` service.
- Generated a permanent (never-expire) System User access token in Meta Business Suite: Business Settings → Users → System Users → `jag-api`, assigned to the **JAG Platform** app (App ID `908891082236842`) with Full access ("Manage app"), permissions `whatsapp_business_messaging` + `whatsapp_business_management`.
- **Blocker along the way:** `developers.facebook.com` repeatedly showed an "Account confirmation needed" checkpoint that errored out ("Sorry, something went wrong") on every attempt to complete it — this blocked the System User asset-assignment step ("App can't be updated... must visit developers.facebook.com/apps to complete confirmation steps"). Cleared only after Robert found and clicked a business-email confirmation link in his own inbox (not the in-app "Confirm Account" button, which kept erroring). Business Suite (`business.facebook.com`) itself was never blocked by this checkpoint — only the developer-platform side was — so most of the WhatsApp Manager / phone-number work was done via Business Suite as a workaround while the developer-platform checkpoint was stuck.
- A payment method also had to be added to the WABA itself (Business Settings → Billing & payments) before phone number registration would proceed — "No payment method found" was blocking the Phone numbers flow even though the number already showed as "Pending" in the list.
- **Root cause of the actual send failures** (`(#133010) Account not registered`): the phone number `+1 868-277-3726` (Phone Number ID `1184193838112120`) had been added to the WhatsApp Business Account but never gone through the Cloud API's one-time `POST /{phone_number_id}/register` call with a 2FA PIN. This is a separate step from just adding the number in the UI, and from the "Two-step verification" toggle in WhatsApp Manager (which is optional/for re-registration security, not required for this step — it appeared greyed out and wasn't needed). PIN chosen: `716042` (saved to password manager).
- After registration, a direct test send (`POST /{phone_number_id}/messages`) returned `200` with a real Meta message ID and correct `wa_id` match — confirmed the sending pipeline works end-to-end from JAG's side.

**Still pending, not yet resolved:** the test recipient's WhatsApp client shows `868-277-3726` as "not on WhatsApp" despite Meta accepting and returning a message ID for messages sent to it. No error, no failed-delivery webhook callback received (JAG's inbound webhook is wired and received nothing). This is most consistent with normal propagation lag for a freshly Cloud-API-registered number — WhatsApp's client-side directory can take hours to recognize a newly-registered number as legitimate, even though the sending API itself works immediately. Re-check in a day or two before treating as a real problem; if it's still not resolved after that, it would warrant a Meta support ticket, since there's no more diagnostic signal available from JAG's side (no error, no webhook, pure wait on Meta's infrastructure).

**How to apply:** don't assume WhatsApp delivery failures are a JAG-side bug without first checking (a) `docker exec jag-api printenv WHATSAPP_ACCESS_TOKEN` is non-empty, (b) the Meta API call itself returns 200 with a message ID (if not, check the error code — `133010` means re-run the register step), and (c) enough time has passed since the number was first registered. The lease-signing / Documenso flow works independently of WhatsApp delivery status — the `sign.jagcorporate.com` link is valid and reachable directly regardless; WhatsApp is only the delivery channel for that link, not a dependency of the signing flow itself.
